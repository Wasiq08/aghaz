import 'package:flutter/material.dart';

class VolunteerPicture extends StatelessWidget {
  const VolunteerPicture({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Picture"),
    );
  }
}